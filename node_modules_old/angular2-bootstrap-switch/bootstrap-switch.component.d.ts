import { ElementRef, EventEmitter, OnChanges, SimpleChange, Renderer, AfterViewInit, AfterViewChecked } from '@angular/core';
export declare class BootstrapSwitchComponent implements OnChanges, AfterViewInit, AfterViewChecked {
    private el;
    private renderer;
    _onSpan: ElementRef;
    _offSpan: ElementRef;
    _midSpan: ElementRef;
    _container: ElementRef;
    _main: ElementRef;
    status: boolean;
    statusChange: EventEmitter<boolean>;
    onText: string;
    offText: string;
    onColor: string;
    offColor: string;
    size: string;
    disabled: boolean;
    _statusStr: string;
    _onColor: string;
    _offColor: string;
    _minWidth: number;
    _sizeClass: string;
    _disabledClass: string;
    _needCalculateWidth: boolean;
    constructor(el: ElementRef, renderer: Renderer);
    toggleStatus(): void;
    private _setDisabled(disabled);
    private _setColor(switchLabel, value);
    private _calculateWidth();
    private _calculateSize();
    ngAfterViewChecked(): void;
    ngOnChanges(changes: {
        [propKey: string]: SimpleChange;
    }): void;
    ngAfterViewInit(): void;
}
